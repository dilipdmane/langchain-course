[project]
name = "under-hood"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jyoti", email = "dilipdmane@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
under-hood = "under_hood:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
